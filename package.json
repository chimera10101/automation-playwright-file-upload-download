{
  "name": "file-upload-download-playwright",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "show-report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}